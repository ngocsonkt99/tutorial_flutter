name: tutorial_flutter
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
    sdk: ">=2.16.0 <3.0.0"

dependencies:
    flutter:
        sdk: flutter
        
    #flutter_svg: ^0.18.0
    photo_view: ^0.12.0
    carousel_slider: ^4.0.0-nullsafety.0
    multi_image_picker: ^4.8.0
    path_provider: ^2.0.1

    flutter_bloc: ^7.0.0
    http: ^0.13.1
    flutter_absolute_path: any # run uploadPhotoLocalStorage
    # timelines: ^0.0.2+1
    image_picker: ^0.7.3
    flutter_slidable: ^0.6.0-nullsafety.0

    # The following adds the Cupertino Icons font to your application.
    # Use with the CupertinoIcons class for iOS style icons.
    cupertino_icons: ^1.0.2

dev_dependencies:
    flutter_test:
        sdk: flutter

    flutter_lints: ^1.0.0

flutter:
    uses-material-design: true

    assets:
        - assets/
        - assets/f1.jpg
        - assets/f2.jpg
        - assets/f3.jpg
